function Settings() {
    return (
        <div className="page">
            <h2>Settings</h2>
            <p>Adjust application preferences.</p>
        </div>
    );
}
// update order for the staff and the order
export default Settings;
